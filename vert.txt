/*
Authors: Daniel Rehberg, Finley Huggins, Henry Jochaniewicz
Date Modified: October 30, 2025
*/

#version 410

layout(location = 0) in vec3 pos;
layout(location = 1) in vec2 uv;

//MOVE TO A UBO
uniform mat4 proj;
uniform mat4 view;
//END MOVE TO A UBO

// TODO: we ultimately want more than just a single square
uniform mat4 model;
uniform usampler2D tex;

smooth out vec2 vTexCoord;

void main()
{
    uvec4 data = texture(tex, uv);

	vTexCoord = uv;
    // the /100 is arbitrary
	gl_Position = vec4(pos.x, pos.y + data.a/100.0, pos.z, 1.0);
}
