/*
Author: Daniel Rehberg
Date Modified: Janurary 26, 2025
*/

#version 410

smooth in vec2 theTexCoord;
out vec4 color;
in vec3 normal;

uniform usampler2D tex;

void main()
{
	uvec4 data = texture(tex, theTexCoord);
	/*vec4 temp = vec4(float(data.r) / 255.0f,
					 float(data.g) / 255.0f,
					 float(data.b) / 255.0f, 1.0f);*/
	vec4 temp = vec4(float(texture(tex, theTexCoord).a) / 255.0);
	temp.a = 1.0;
	color = temp;
}
